# Консольний погодний бот (Weather Bot)

## 🌦️ Опис
Це простий консольний Python-скрипт, який показує поточну погоду для обраного міста. Він використовує відкритий API від [OpenWeatherMap](https://openweathermap.org/), щоб отримувати актуальні дані.

## ⚙️ Використані технології
* **Мова:** Python
* **Бібліотеки:**
    * `requests` (для виконання HTTP-запитів до API)
* **Сервіси:**
    * OpenWeatherMap API

## 🚀 Як запустити програму
Щоб запустити цей скрипт локально, вам потрібно виконати наступні кроки:

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [ПОСИЛАННЯ_НА_ТВІЙ_РЕПОЗИТОРІЙ]
    cd [НАЗВА_ТВОГО_РЕПОЗИТОРІЯ]
    ```

2.  **(Рекомендовано) Створіть та активуйте віртуальне середовище:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # для Linux/Mac
    venv\Scripts\activate     # для Windows
    ```

3.  **Встановіть необхідні бібліотеки:**
    ```bash
    pip install requests
    ```

4.  **Отримайте свій API ключ:**
    * Зареєструйтесь на [OpenWeatherMap](https://openweathermap.org/).
    * Перейдіть у розділ "My API keys" та скопіюйте свій ключ.

5.  **Відредагуйте код:**
    * Відкрийте файл `weather.py`.
    * Знайдіть рядок `API_KEY = "YOUR_API_KEY_HERE"` та вставте у лапки свій ключ.
    * За бажанням, змініть `CITY_NAME = "Kyiv"` на будь-яке інше місто.

6.  **Запустіть скрипт:**
    ```bash
    python weather.py
    ```